$ const labels = input.labels && Array.isArray(input.labels) ? input.labels : [];
<if(labels.includes('Supplier Submitted'))>
  <p>
    <i>This content was written and submitted by the supplier. It has only been modified to comply with this publication’s space and style.</i>
  </p>
</if>
